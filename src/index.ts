import { ISelector } from "./selector/index.d";

export default (selector: ISelector, data: any): boolean => {
  /* istanbul ignore next */
  throw ReferenceError("Could not compile code for some reason!");
}
